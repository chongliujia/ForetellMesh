[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "foretellmesh"
version = "0.1.0"
description = "Reproducible financial event forecasting research"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
parquet = ["pyarrow>=15"]
archive = ["pyarrow>=15", "zstandard>=0.22"]
graph = ["langgraph==1.2.11"]
rl = ["gymnasium==1.2.3", "stable-baselines3==2.7.1", "sb3-contrib==2.7.1"]

[project.scripts]
foretellmesh = "foretellmesh.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
